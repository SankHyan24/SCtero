version: '3.8'

services:
  sctero:
    build: .
    container_name: sctero
    network_mode: host
    restart: unless-stopped
    environment:
      - BASE_DIR=/data
      - ADMIN_PASSWORD=admin
      - SECRET_KEY=your-super-secret-key
      - PORT=5000
    volumes:
      # Mount host path from config.json to the /data dir in container
      - /data/appsdata/sctero:/data
